# Инструкция по миграции: создание таблицы admins

## ✅ Что уже сделано

1. ✅ Создан SQL скрипт для таблицы `admins` (`database/create-admins-table.sql`)
2. ✅ Создан endpoint `/api/setup-admin` для создания записи админа
3. ✅ Создан endpoint `/api/admin-profile.js` для работы с профилем админа
4. ✅ Исправлен `guide-profile.js` (заменен `user_id` на `id`)
5. ✅ Исправлен `tours.js` (заменен `user_id` на `id` в запросах к `guides`)
6. ✅ Обновлен `AdminLoginPage.jsx` для работы с БД (с fallback на хардкод)

## 📋 Шаги миграции

### Шаг 1: Создать таблицу `admins` в Supabase

1. Откройте Supabase Dashboard → SQL Editor
2. Скопируйте содержимое файла `database/create-admins-table.sql`:

```sql
-- Create admins table for admin profiles
-- This table stores additional profile data for admins
-- The id column matches users.id (same as guides table structure)

CREATE TABLE IF NOT EXISTS admins (
  id UUID PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
  name VARCHAR(255) NOT NULL,
  bio TEXT,
  avatar_url VARCHAR(500),
  permissions JSONB,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Create index for faster lookups
CREATE INDEX IF NOT EXISTS idx_admins_id ON admins(id);

-- Add comment to table
COMMENT ON TABLE admins IS 'Admin profiles - additional data for users with role=admin. id matches users.id';
```

3. Выполните SQL скрипт (нажмите "Run")

### Шаг 2: Создать запись админа в БД

После деплоя backend на Vercel, вызовите endpoint:

**Вариант A: Через curl (терминал)**
```bash
curl -X POST https://fliptripback.vercel.app/api/setup-admin \
  -H "Content-Type: application/json" \
  -d '{
    "email": "yes.stroynov@gmail.com",
    "password": "fliptrip13",
    "name": "Admin"
  }'
```

**Вариант B: Через браузер (JavaScript консоль)**
```javascript
fetch('https://fliptripback.vercel.app/api/setup-admin', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    email: 'yes.stroynov@gmail.com',
    password: 'fliptrip13',
    name: 'Admin'
  })
})
.then(r => r.json())
.then(console.log)
.catch(console.error);
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "message": "Admin setup completed",
  "admin": {
    "id": "uuid-здесь",
    "email": "yes.stroynov@gmail.com",
    "name": "Admin",
    "role": "admin"
  },
  "created": {
    "user": true,
    "profile": true
  }
}
```

### Шаг 3: Проверить создание записи

**Проверить через endpoint:**
```bash
curl https://fliptripback.vercel.app/api/check-admins
```

**Или в Supabase:**
1. Откройте Table Editor
2. Проверьте таблицу `users` - должна быть запись с `email = 'yes.stroynov@gmail.com'` и `role = 'admin'`
3. Проверьте таблицу `admins` - должна быть запись с `id` совпадающим с `users.id`

### Шаг 4: Протестировать логин

1. Откройте `/admin/login` на фронтенде
2. Введите:
   - Email: `yes.stroynov@gmail.com`
   - Password: `fliptrip13`
3. Нажмите "Login"
4. Должно произойти:
   - Логин через `/api/auth-login` (БД)
   - Сохранение токена и данных пользователя
   - Редирект на `/admin/dashboard`

**Если логин не работает:**
- Проверьте, что таблица `admins` создана
- Проверьте, что запись создана через `/api/setup-admin`
- Проверьте логи Vercel для ошибок
- Хардкод-логин должен работать как fallback

## 🔍 Проверка структуры

### Таблица `users`
- Должна содержать запись с:
  - `email = 'yes.stroynov@gmail.com'`
  - `role = 'admin'`
  - `is_active = true`
  - `password_hash` (захешированный пароль)

### Таблица `admins`
- Должна содержать запись с:
  - `id` = `users.id` (для админа)
  - `name = 'Admin'` (или другое имя)

### Таблица `guides`
- Структура не меняется
- Использует `id` (не `user_id`) = `users.id`

## 🛠️ Что изменилось в коде

### Backend

1. **`api/guide-profile.js`**
   - Заменен `.eq('user_id', userId)` → `.eq('id', userId)`
   - Использует `guides.id = users.id` (не `user_id`)

2. **`api/tours.js`**
   - Заменен `.eq('user_id', tour.guide_id)` → `.eq('id', tour.guide_id)`
   - Использует `guides.id = users.id` (не `user_id`)

3. **`api/admin-profile.js`** (новый)
   - Аналогичен `guide-profile.js`, но для таблицы `admins`
   - GET - получить профиль админа
   - PUT - обновить/создать профиль админа

4. **`api/setup-admin.js`** (новый)
   - Создает запись в `users` (если не существует)
   - Создает запись в `admins` (если не существует)
   - Хеширует пароль

### Frontend

1. **`AdminLoginPage.jsx`**
   - Сначала пытается логин через `/api/auth-login` (БД)
   - Если не получается, использует хардкод как fallback
   - После хардкод-логина пытается создать запись через `/api/setup-admin`

## ⚠️ Важные замечания

1. **Не удаляйте записи из `users`** - они нужны для аутентификации
2. **Таблица `users` остается для всех** - user, guide, creator, admin
3. **Таблицы `guides` и `admins`** - только для дополнительных данных профиля
4. **Хардкод-логин сохранен** - как fallback на случай проблем
5. **Все логинятся через `users`** - единая точка аутентификации

## 🐛 Troubleshooting

### Ошибка: "Admins table does not exist"
- **Решение:** Выполните SQL скрипт из `database/create-admins-table.sql` в Supabase

### Ошибка: "column guides.user_id does not exist"
- **Решение:** Уже исправлено в коде, использует `guides.id` (не `user_id`)

### Логин не работает
- **Проверьте:**
  1. Таблица `admins` создана?
  2. Запись создана через `/api/setup-admin`?
  3. Пароль правильный?
  4. `is_active = true` в `users`?

### Хардкод-логин не работает
- **Проверьте:** Код должен работать как fallback, если БД недоступна

## 📝 Следующие шаги (опционально)

После успешной миграции можно:
1. Создать страницу настроек админа (используя `/api/admin-profile`)
2. Добавить поля в таблицу `admins` (например, `permissions`)
3. Убрать хардкод-логин (если уверены, что БД работает)

## ✅ Чеклист миграции

- [ ] Создана таблица `admins` в Supabase
- [ ] Вызван `/api/setup-admin` для создания записи
- [ ] Проверена запись в таблице `users` (email, role, is_active)
- [ ] Проверена запись в таблице `admins` (id совпадает с users.id)
- [ ] Протестирован логин через `/admin/login`
- [ ] Проверена работа админ-панели
- [ ] Проверена работа других функций (не сломалось ли что-то)



