# Диагностика проблемы с tours-update endpoint

## Проблема
- DELETE запросы возвращают 404
- PUT запросы возвращают 404
- Endpoint не вызывается

## Что нужно проверить

### 1. Проверка доступности endpoint
Откройте в браузере:
```
https://fliptripback.vercel.app/api/tours-update?test=true
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "message": "tours-update endpoint is working!",
  "method": "GET",
  "url": "/api/tours-update?test=true",
  "timestamp": "2024-..."
}
```

**Если 404:**
- Vercel не видит файл `api/tours-update.js`
- Проблема с деплоем или кэшированием

### 2. Проверка логов Vercel

1. Откройте Vercel Dashboard
2. Выберите проект `fliptripback`
3. Перейдите в раздел **Logs**
4. Попробуйте удалить или обновить тур
5. Проверьте, появляются ли логи:
   - `🚀 tours-update handler called: DELETE /api/tours-update?id=...`
   - `🚀 tours-update handler called: PUT /api/tours-update?id=...`

**Если логов НЕТ:**
- Endpoint не вызывается
- Проблема с роутингом Vercel

**Если логи ЕСТЬ:**
- Endpoint вызывается, но что-то не так в коде
- Нужно посмотреть на ошибки в логах

### 3. Проверка структуры файлов

Убедитесь, что файл существует:
```
api/tours-update.js
```

И что он экспортирует handler:
```javascript
export default async function handler(req, res) {
  // ...
}
```

### 4. Проверка настроек Vercel

Возможно, нужно проверить:
- Настройки Build & Development Settings
- Настройки Functions
- Версию Node.js

## Что прислать для диагностики

1. **Скриншот логов Vercel** при попытке удалить/обновить тур
2. **Результат тестового endpoint** (GET /api/tours-update?test=true)
3. **Скриншот структуры файлов** в Vercel (если доступно)
4. **Любые ошибки из логов Vercel**, связанные с tours-update

