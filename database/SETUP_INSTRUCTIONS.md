# 🗄️ Database Setup Instructions

## Шаг 1: Создание Supabase проекта

1. Перейди на https://supabase.com
2. Зарегистрируйся или войди в аккаунт
3. Нажми **"New Project"**
4. Заполни:
   - **Name**: `fliptrip-db` (или любое другое)
   - **Database Password**: Сохрани пароль! (он понадобится)
   - **Region**: Выбери ближайший (например, `West US`)
5. Нажми **"Create new project"**
6. Подожди 2-3 минуты, пока проект создается

## Шаг 2: Получение credentials

1. В проекте Supabase перейди в **Settings** → **API**
2. Скопируй следующие значения:

   - **Project URL** → это будет `SUPABASE_URL`
     ```
     https://xxxxx.supabase.co
     ```

   - **service_role** key (секретный ключ) → это будет `SUPABASE_SERVICE_ROLE_KEY`
     ```
     eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
     ```
     ⚠️ **Важно**: Используй именно `service_role` key, а не `anon` key!

## Шаг 3: Запуск SQL схемы

1. В Supabase Dashboard перейди в **SQL Editor** (левый сайдбар)
2. Нажми **"New query"**
3. Открой файл `database/schema.sql` из этого проекта
4. Скопируй **весь** содержимое файла
5. Вставь в SQL Editor в Supabase
6. Нажми **"Run"** (или `Cmd/Ctrl + Enter`)
7. Должно появиться сообщение **"Success. No rows returned"** - это нормально

## Шаг 4: Настройка Environment Variables в Vercel

1. Перейди в Vercel Dashboard → твой проект `fliptrip-clean-backend`
2. Перейди в **Settings** → **Environment Variables**
3. Добавь следующие переменные:

   ```
   SUPABASE_URL=https://xxxxx.supabase.co
   SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ```

4. Нажми **"Save"**
5. **Важно**: Передеплой проект, чтобы переменные применились:
   - Перейди в **Deployments**
   - Нажми **"..."** на последнем деплое → **"Redeploy"**

## Шаг 5: Проверка подключения

После деплоя проверь, что БД подключена:

1. Открой в браузере: `https://fliptripback.vercel.app/api/admin-stats`
2. Должен вернуться JSON с статистикой (даже если все значения 0)

## Что дальше?

После успешной настройки БД:

1. ✅ **Админ-панель** - создадим frontend модуль для управления БД
2. ✅ **Миграция туров** - обновим `tours-create.js` и `tours-update.js` для работы с БД
3. ✅ **Генератор расписаний** - обновим `smart-itinerary.js` для поиска локаций в БД
4. ✅ **Экспорт данных** - добавим функцию экспорта в CSV/Excel

## Полезные ссылки

- **Supabase Dashboard**: https://app.supabase.com
- **SQL Editor**: Для выполнения запросов и просмотра данных
- **Table Editor**: Визуальный редактор таблиц (полезно для админов)
- **API Docs**: Автоматически генерируемая документация API

## Troubleshooting

### Ошибка "relation does not exist"
- Убедись, что SQL схема выполнена полностью
- Проверь в Supabase Dashboard → **Table Editor**, что таблицы созданы

### Ошибка "Invalid API key"
- Проверь, что используешь `service_role` key, а не `anon` key
- Убедись, что переменные окружения правильно установлены в Vercel

### Ошибка подключения
- Проверь, что `SUPABASE_URL` правильный (должен заканчиваться на `.supabase.co`)
- Убедись, что проект не приостановлен (бесплатные проекты могут приостанавливаться после неактивности)

